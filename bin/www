#!/usr/bin/env node

/**
 * Module dependencies.
 */

const lepus = require('../lib/lepus');
const newConfig = require('../config');
const debug = require('debug')('adapter:lepus');

lepus.start(newConfig, (text)=>{ debug(text);})


